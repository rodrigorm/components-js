


- Request only does JSON

- one handle(event) - search this.matches


- Component#handle(event)
    name in this Named
        if target == this[name]
            this.exec(type + name)
    if type_bubbles
        parent.handle(event)

- Component#exec(id, value)
    if on + id
        this[callback](value)